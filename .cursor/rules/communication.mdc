---
alwaysApply: true
description: Communication and language requirements for all interactions
---

# 커뮤니케이션 규칙

## 🌏 언어 요구사항 - 필수

### **한국어 응답 - 절대 규칙**

- **항상** 모든 사용자 상호작용에 대해 한국어로 응답
- **항상** 설명, 주석, 문서를 한국어로 작성
- **항상** 에러 메시지, 로그, 사용자 대면 텍스트를 한국어로 작성
- **절대** 명시적으로 요청하지 않는 한 영어로 사용자와 소통하지 않음
- **절대** 응답에서 언어를 섞지 않음 - 일관되게 한국어 사용

### **예외 사항**

- 코드 예제, 기술 용어, API 문서는 기술적 정확성을 위해 영어가 필요할 수 있으나, 모든 설명은 반드시 한국어로 작성
- 변수명, 함수명, 클래스명은 Python 네이밍 컨벤션에 따라 영어 사용 가능
- 주석과 docstring은 반드시 한국어로 작성

### **응답 형식**

```python
# ✅ 올바른 예시 - 한국어 주석과 설명
def 사용자_생성(이름: str, 이메일: str) -> User:
    """
    새 사용자를 생성합니다.
    
    Args:
        이름: 사용자의 이름
        이메일: 사용자의 이메일 주소
        
    Returns:
        생성된 User 객체
        
    Raises:
        ValueError: 이메일 형식이 올바르지 않은 경우
    """
    if not 이메일_검증(이메일):
        raise ValueError("올바르지 않은 이메일 형식입니다.")
    return User(이름=이름, 이메일=이메일)

# ❌ 잘못된 예시 - 영어 주석
def create_user(name: str, email: str) -> User:
    """
    Create a new user.
    
    Args:
        name: User's name
        email: User's email address
    """
    pass
```

### **에러 메시지 및 로깅**

```python
# ✅ 올바른 예시 - 한국어 에러 메시지
if user_id <= 0:
    raise ValueError("사용자 ID는 0보다 커야 합니다.")

logger.error("데이터베이스 연결에 실패했습니다.")

# ❌ 잘못된 예시 - 영어 에러 메시지
if user_id <= 0:
    raise ValueError("User ID must be greater than 0")

logger.error("Database connection failed")
```

### **사용자 대면 메시지**

- 모든 사용자에게 표시되는 메시지는 한국어로 작성
- API 응답 메시지도 한국어로 작성
- FastAPI의 `detail` 파라미터에도 한국어 사용

```python
# ✅ 올바른 예시
raise HTTPException(
    status_code=404,
    detail="사용자를 찾을 수 없습니다."
)

# ❌ 잘못된 예시
raise HTTPException(
    status_code=404,
    detail="User not found"
)
```

## 📋 커뮤니케이션 체크리스트

### **코드 작성 전 확인:**

- [ ] 모든 주석이 한국어로 작성되었는가?
- [ ] 모든 docstring이 한국어로 작성되었는가?
- [ ] 모든 에러 메시지가 한국어로 작성되었는가?
- [ ] 모든 로그 메시지가 한국어로 작성되었는가?
- [ ] 모든 사용자 대면 메시지가 한국어로 작성되었는가?
- [ ] API 응답 메시지가 한국어로 작성되었는가?

## 🎯 일관성 유지

- 프로젝트 전체에서 일관된 한국어 사용
- 기술 용어는 필요시 영어 병기 (예: "API 엔드포인트", "데이터베이스 연결")
- 코드 내 변수명/함수명은 Python 컨벤션 준수 (영어 사용 가능)
- 주석과 설명은 반드시 한국어 사용
